<header id="header">
  <div class="title-holder">
    <div class="title">
      <%= link_to image_tag('PetStopLogo-hand.jpg'), url_for({:controller => 'welcome', :action => 'index'}) %>
    </div>
  </div>
</header>
<div class>
  <div class="page-header">

      <%= form_tag(root_path, :method => "get", id: "search-form") do %>
      <div class="input-group small-2 large-4 columns">
        <%= text_field_tag :search, params[:search], placeholder: "Search Vets by Name", class: "input-group-field" %>
        <div class="input-group-button button">
          <%= submit_tag "Search", class: "button" %>
        </div>
      </div>
      <% end %>
      <div id="vet-info">
        <% if params[:search] %>
          <% if @vets.present? %>
            <%= render "vet" %>
          <% else %>
            <p>There are no vets containing the term(s):
              <%= params[:search] %></p>
          <% end %>
        <% end %>
        <div class="welcome-buttons">
          <%= link_to "See all Vets", vets_path, method: :get, class: "button info" %>
        </div>
      </div>
  </div>
